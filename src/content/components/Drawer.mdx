---
name: 'Drawer'
description: 'Permite al usuario mostrar y ocultar contenido en una ventana flotante.'
---

import CodeBlock from '@/components/landing/CodeBlock.jsx'
import Preview from '@/components/landing/Preview.astro'
import CustomDrawer from '@/components/react/Drawer/CustomDrawer.jsx'
import CodeTabs from '@/components/landing/CodeTabs.jsx'
import {
  DrawerReact,
  DrawerContentReact,
  DrawerHeaderReact,
  DrawerFooterReact,
  DrawerBodyReact
} from '@/const/componentsReact'
import {
  DrawerTs,
  DrawerContentTs,
  DrawerHeaderTs,
  DrawerFooterTs,
  DrawerBodyTs
} from '@/const/componentsTs'
import {
  DrawerAstro,
  DrawerContentAstro,
  DrawerHeaderAstro,
  DrawerFooterAstro,
  DrawerBodyAstro
} from '@/const/componentsAstro'

## Sobre el componente

Este componente se utiliza para mostrar un panel que se desliza desde un borde de la pantalla y contiene información adicional.

## Uso básico

Para utilizar el componente, debemos utilizar los componentes `Drawer`, `DrawerContent`, `DrawerHeader`, `DrawerBody` y `DrawerFooter`.

<Preview>
  <div class='flex flex-col items-center justify-center gap-4 h-96'>
    <CustomDrawer client:load />
  </div>
</Preview>

<CodeBlock client:load>

```jsx
const [isDrawerOpen, setDrawerOpen] = useState(false)

return (
  <div>
    <Button onClick={() => setDrawerOpen(true)}>Open Drawer</Button>
    <Drawer isOpen={isDrawerOpen} onClose={() => setDrawerOpen(false)}>
      <DrawerContent>
        <DrawerHeader
          onClose={() => setDrawerOpen(false)}
          closeDrawer={() => setDrawerOpen(false)}
        >
          Drawer Title
        </DrawerHeader>
        <DrawerBody>
          <p>This is the body of the drawer.</p>
        </DrawerBody>
        <DrawerFooter>
          <Button onClick={() => setDrawerOpen(false)}>Close</Button>
        </DrawerFooter>
      </DrawerContent>
    </Drawer>
  </div>
)
```

</CodeBlock>

Y el código para el componente `Drawer` es el siguiente:

<CodeTabs
  client:load
  tabs={[
    { label: 'React', code: DrawerReact },
    { label: 'TypeScript', code: DrawerTs },
    { label: 'Astro', code: DrawerAstro }
  ]}
/>

el código para el componente `DrawerHeader` es el siguiente:

<CodeTabs
  client:load
  tabs={[
    { label: 'React', code: DrawerHeaderReact },
    { label: 'TypeScript', code: DrawerHeaderTs },
    { label: 'Astro', code: DrawerHeaderAstro }
  ]}
/>

el código para el componente `DrawerContent` es el siguiente:

<CodeTabs
  client:load
  tabs={[
    { label: 'React', code: DrawerContentReact },
    { label: 'TypeScript', code: DrawerContentTs },
    { label: 'Astro', code: DrawerContentAstro }
  ]}
/>

el código para el componente `DrawerBody` es el siguiente:

<CodeTabs
  client:load
  tabs={[
    { label: 'React', code: DrawerBodyReact },
    { label: 'TypeScript', code: DrawerBodyTs },
    { label: 'Astro', code: DrawerBodyAstro }
  ]}
/>

el código para el componente `DrawerFooter` es el siguiente:

<CodeTabs
  client:load
  tabs={[
    { label: 'React', code: DrawerFooterReact },
    { label: 'TypeScript', code: DrawerFooterTs },
    { label: 'Astro', code: DrawerFooterAstro }
  ]}
/>

## Cerrar Drawer

Por defecto el componente `Drawer` puede ser cerrado con la tecla `Escape` o haciendo clic fuera del componente. Para deshabilitar esto, simplemente debes agregar el atributo `isDismissable={false}`.

<Preview>
  <div class='flex flex-col items-center justify-center gap-4 h-96'>
    <CustomDrawer client:load isDismissable={false} />
  </div>
</Preview>

<CodeBlock client:load>

```jsx
<Drawer
  isOpen={isDrawerOpen}
  onClose={() => setDrawerOpen(false)}
  isDismissable={true}
>
  <DrawerContent>
    <DrawerHeader
      onClose={() => setDrawerOpen(false)}
      closeDrawer={() => setDrawerOpen(false)}
    >
      Drawer Title
    </DrawerHeader>
    <DrawerBody>
      <p>This is the body of the drawer.</p>
    </DrawerBody>
    <DrawerFooter>
      <Button onClick={() => setDrawerOpen(false)}>Close</Button>
    </DrawerFooter>
  </DrawerContent>
</Drawer>
```

</CodeBlock>

## Posicionamiento

Para cambiar el posicionamiento del componente, se puede utilizar el atributo `position` en el componente `Drawer`. Este atributo acepta cuatro valores: `top`, `bottom`, `left` y `right`.

<Preview>
  <div class='flex flex-col items-center justify-center gap-4 h-96'>
    <CustomDrawer client:load position='top' text='Open Drawer Top' />
    <CustomDrawer client:load position='bottom' text='Open Drawer Bottom' />
    <CustomDrawer client:load position='left' text='Open Drawer Left' />
    <CustomDrawer client:load position='right' text='Open Drawer Right' />
  </div>
</Preview>

<CodeBlock client:load>

```jsx
<Drawer
  isOpen={isDrawerOpen}
  onClose={() => setDrawerOpen(false)}
  position='top'
>
  <DrawerContent>
    <DrawerHeader
      onClose={() => setDrawerOpen(false)}
      closeDrawer={() => setDrawerOpen(false)}
    >
      Drawer Title
    </DrawerHeader>
    <DrawerBody>
      <p>This is the body of the drawer.</p>
    </DrawerBody>
    <DrawerFooter>
      <Button onClick={() => setDrawerOpen(false)}>Close</Button>
    </DrawerFooter>
  </DrawerContent>
</Drawer>

<Drawer
  isOpen={isDrawerOpen}
  onClose={() => setDrawerOpen(false)}
  position='bottom'
>
  <DrawerContent>
    <DrawerHeader
      onClose={() => setDrawerOpen(false)}
      closeDrawer={() => setDrawerOpen(false)}
    >
      Drawer Title
    </DrawerHeader>
    <DrawerBody>
      <p>This is the body of the drawer.</p>
    </DrawerBody>
    <DrawerFooter>
      <Button onClick={() => setDrawerOpen(false)}>Close</Button>
    </DrawerFooter>
  </DrawerContent>
</Drawer>

<Drawer
  isOpen={isDrawerOpen}
  onClose={() => setDrawerOpen(false)}
  position='left'
>
  <DrawerContent>
    <DrawerHeader
      onClose={() => setDrawerOpen(false)}
      closeDrawer={() => setDrawerOpen(false)}
    >
      Drawer Title
    </DrawerHeader>
    <DrawerBody>
      <p>This is the body of the drawer.</p>
    </DrawerBody>
    <DrawerFooter>
      <Button onClick={() => setDrawerOpen(false)}>Close</Button>
    </DrawerFooter>
  </DrawerContent>
</Drawer>

<Drawer
  isOpen={isDrawerOpen}
  onClose={() => setDrawerOpen(false)}
  position='right'
>
  <DrawerContent>
    <DrawerHeader
      onClose={() => setDrawerOpen(false)}
      closeDrawer={() => setDrawerOpen(false)}
    >
      Drawer Title
    </DrawerHeader>
    <DrawerBody>
      <p>This is the body of the drawer.</p>
    </DrawerBody>
    <DrawerFooter>
      <Button onClick={() => setDrawerOpen(false)}>Close</Button>
    </DrawerFooter>
  </DrawerContent>
</Drawer>
```

</CodeBlock>

## Efectos

Para cambiar el efecto del componente, se puede utilizar el atributo `effect` en el componente `Drawer`. Este atributo acepta los siguientes valores: `opaque`, `blur` y `transparent`.

<Preview>
  <div class='flex flex-col items-center justify-center gap-4 h-96'>
    <CustomDrawer client:load effect='opaque' text='Open Drawer Opaque' />
    <CustomDrawer client:load effect='blur' text='Open Drawer Blur' />
    <CustomDrawer
      client:load
      effect='transparent'
      text='Open Drawer Transparent'
    />
  </div>
</Preview>

<CodeBlock client:load>

```jsx
<Drawer
  isOpen={isDrawerOpen}
  onClose={() => setDrawerOpen(false)}
  effect='opaque'
>
  <DrawerContent>
    <DrawerHeader
      onClose={() => setDrawerOpen(false)}
      closeDrawer={() => setDrawerOpen(false)}
    >
      Drawer Title
    </DrawerHeader>
    <DrawerBody>
      <p>This is the body of the drawer.</p>
    </DrawerBody>
    <DrawerFooter>
      <Button onClick={() => setDrawerOpen(false)}>Close</Button>
    </DrawerFooter>
  </DrawerContent>
</Drawer>

<Drawer
  isOpen={isDrawerOpen}
  onClose={() => setDrawerOpen(false)}
  effect='blur'
>
  <DrawerContent>
    <DrawerHeader
      onClose={() => setDrawerOpen(false)}
      closeDrawer={() => setDrawerOpen(false)}
    >
      Drawer Title
    </DrawerHeader>
    <DrawerBody>
      <p>This is the body of the drawer.</p>
    </DrawerBody>
    <DrawerFooter>
      <Button onClick={() => setDrawerOpen(false)}>Close</Button>
    </DrawerFooter>
  </DrawerContent>
</Drawer>

<Drawer
  isOpen={isDrawerOpen}
  onClose={() => setDrawerOpen(false)}
  effect='transparent'
>
  <DrawerContent>
    <DrawerHeader
      onClose={() => setDrawerOpen(false)}
      closeDrawer={() => setDrawerOpen(false)}
    >
      Drawer Title
    </DrawerHeader>
    <DrawerBody>
      <p>This is the body of the drawer.</p>
    </DrawerBody>
    <DrawerFooter>
      <Button onClick={() => setDrawerOpen(false)}>Close</Button>
    </DrawerFooter>
  </DrawerContent>
</Drawer>
```

</CodeBlock>

## Tamaños

Para cambiar el tamaño del componente, se puede utilizar el atributo `size` en el componente `Drawer`. Este atributo acepta los siguientes valores: `sm`, `md`, `lg`, `xl` y `full`.

<Preview>
  <div class='flex flex-col items-center justify-center gap-4 h-96'>
    <CustomDrawer client:load size='sm' text='Open Drawer Sm' />
    <CustomDrawer client:load size='md' text='Open Drawer Md' />
    <CustomDrawer client:load size='lg' text='Open Drawer Lg' />
    <CustomDrawer client:load size='xl' text='Open Drawer Xl' />
    <CustomDrawer client:load size='full' text='Open Drawer full' />
  </div>
</Preview>

<CodeBlock client:load>

```jsx
<Drawer
  isOpen={isDrawerOpen}
  onClose={() => setDrawerOpen(false)}
  size='sm'
>
  <DrawerContent>
    <DrawerHeader
      onClose={() => setDrawerOpen(false)}
      closeDrawer={() => setDrawerOpen(false)}
    >
      Drawer Title
    </DrawerHeader>
    <DrawerBody>
      <p>This is the body of the drawer.</p>
    </DrawerBody>
    <DrawerFooter>
      <Button onClick={() => setDrawerOpen(false)}>Close</Button>
    </DrawerFooter>
  </DrawerContent>
</Drawer>

<Drawer
  isOpen={isDrawerOpen}
  onClose={() => setDrawerOpen(false)}
  size='md'
>
  <DrawerContent>
    <DrawerHeader
      onClose={() => setDrawerOpen(false)}
      closeDrawer={() => setDrawerOpen(false)}
    >
      Drawer Title
    </DrawerHeader>
    <DrawerBody>
      <p>This is the body of the drawer.</p>
    </DrawerBody>
    <DrawerFooter>
      <Button onClick={() => setDrawerOpen(false)}>Close</Button>
    </DrawerFooter>
  </DrawerContent>
</Drawer>

<Drawer
  isOpen={isDrawerOpen}
  onClose={() => setDrawerOpen(false)}
  size='lg'
>
  <DrawerContent>
    <DrawerHeader
      onClose={() => setDrawerOpen(false)}
      closeDrawer={() => setDrawerOpen(false)}
    >
      Drawer Title
    </DrawerHeader>
    <DrawerBody>
      <p>This is the body of the drawer.</p>
    </DrawerBody>
    <DrawerFooter>
      <Button onClick={() => setDrawerOpen(false)}>Close</Button>
    </DrawerFooter>
  </DrawerContent>
</Drawer>

<Drawer
  isOpen={isDrawerOpen}
  onClose={() => setDrawerOpen(false)}
  size='xl'
>
  <DrawerContent>
    <DrawerHeader
      onClose={() => setDrawerOpen(false)}
      closeDrawer={() => setDrawerOpen(false)}
    >
      Drawer Title
    </DrawerHeader>
    <DrawerBody>
      <p>This is the body of the drawer.</p>
    </DrawerBody>
    <DrawerFooter>
      <Button onClick={() => setDrawerOpen(false)}>Close</Button>
    </DrawerFooter>
  </DrawerContent>
</Drawer>

<Drawer
  isOpen={isDrawerOpen}
  onClose={() => setDrawerOpen(false)}
  size='full'
>
  <DrawerContent>
    <DrawerHeader
      onClose={() => setDrawerOpen(false)}
      closeDrawer={() => setDrawerOpen(false)}
    >
      Drawer Title
    </DrawerHeader>
    <DrawerBody>
      <p>This is the body of the drawer.</p>
    </DrawerBody>
    <DrawerFooter>
      <Button onClick={() => setDrawerOpen(false)}>Close</Button>
    </DrawerFooter>
  </DrawerContent>
</Drawer>
```

</CodeBlock>

## Color

Este componente tiene un atributo para cambiar su color. Para esto tenemos los siguientes valores: `default`, `primary`, `secondary`, `success`, `warning` y `danger`.

<Preview>
  <div class='flex flex-col items-center justify-center gap-4 h-96'>
    <CustomDrawer client:load color='default' text='Open Drawer Default' />
    <CustomDrawer client:load color='primary' text='Open Drawer Primary' />
    <CustomDrawer client:load color='secondary' text='Open Drawer Secondary' />
    <CustomDrawer client:load color='success' text='Open Drawer Success' />
    <CustomDrawer client:load color='warning' text='Open Drawer Warning' />
    <CustomDrawer client:load color='danger' text='Open Drawer Danger' />
  </div>
</Preview>

<CodeBlock client:load>

```jsx
<Drawer
  isOpen={isDrawerOpen}
  onClose={() => setDrawerOpen(false)}
  color='default'
>
  <DrawerContent>
    <DrawerHeader
      onClose={() => setDrawerOpen(false)}
      closeDrawer={() => setDrawerOpen(false)}
    >
      Drawer Title
    </DrawerHeader>
    <DrawerBody>
      <p>This is the body of the drawer.</p>
    </DrawerBody>
    <DrawerFooter>
      <Button onClick={() => setDrawerOpen(false)}>Close</Button>
    </DrawerFooter>
  </DrawerContent>
</Drawer>

<Drawer
  isOpen={isDrawerOpen}
  onClose={() => setDrawerOpen(false)}
  color='primary'
>
  <DrawerContent>
    <DrawerHeader
      onClose={() => setDrawerOpen(false)}
      closeDrawer={() => setDrawerOpen(false)}
    >
      Drawer Title
    </DrawerHeader>
    <DrawerBody>
      <p>This is the body of the drawer.</p>
    </DrawerBody>
    <DrawerFooter>
      <Button onClick={() => setDrawerOpen(false)}>Close</Button>
    </DrawerFooter>
  </DrawerContent>
</Drawer>

<Drawer
  isOpen={isDrawerOpen}
  onClose={() => setDrawerOpen(false)}
  color='secondary'
>
  <DrawerContent>
    <DrawerHeader
      onClose={() => setDrawerOpen(false)}
      closeDrawer={() => setDrawerOpen(false)}
    >
      Drawer Title
    </DrawerHeader>
    <DrawerBody>
      <p>This is the body of the drawer.</p>
    </DrawerBody>
    <DrawerFooter>
      <Button onClick={() => setDrawerOpen(false)}>Close</Button>
    </DrawerFooter>
  </DrawerContent>
</Drawer>

<Drawer
  isOpen={isDrawerOpen}
  onClose={() => setDrawerOpen(false)}
  color='success'
>
  <DrawerContent>
    <DrawerHeader
      onClose={() => setDrawerOpen(false)}
      closeDrawer={() => setDrawerOpen(false)}
    >
      Drawer Title
    </DrawerHeader>
    <DrawerBody>
      <p>This is the body of the drawer.</p>
    </DrawerBody>
    <DrawerFooter>
      <Button onClick={() => setDrawerOpen(false)}>Close</Button>
    </DrawerFooter>
  </DrawerContent>
</Drawer>

<Drawer
  isOpen={isDrawerOpen}
  onClose={() => setDrawerOpen(false)}
  color='warning'
>
  <DrawerContent>
    <DrawerHeader
      onClose={() => setDrawerOpen(false)}
      closeDrawer={() => setDrawerOpen(false)}
    >
      Drawer Title
    </DrawerHeader>
    <DrawerBody>
      <p>This is the body of the drawer.</p>
    </DrawerBody>
    <DrawerFooter>
      <Button onClick={() => setDrawerOpen(false)}>Close</Button>
    </DrawerFooter>
  </DrawerContent>
</Drawer>

<Drawer
  isOpen={isDrawerOpen}
  onClose={() => setDrawerOpen(false)}
  color='danger'
>
  <DrawerContent>
    <DrawerHeader
      onClose={() => setDrawerOpen(false)}
      closeDrawer={() => setDrawerOpen(false)}
    >
      Drawer Title
    </DrawerHeader>
    <DrawerBody>
      <p>This is the body of the drawer.</p>
    </DrawerBody>
    <DrawerFooter>
      <Button onClick={() => setDrawerOpen(false)}>Close</Button>
    </DrawerFooter>
  </DrawerContent>
</Drawer>
```

</CodeBlock>
