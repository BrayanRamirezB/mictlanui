---
name: Input
description: Permite a los usuarios ingresar información en un campo de texto.
---

import Preview from '@/components/Preview.astro'
import CodeBlock from '@/components/CodeBlock.jsx'
import Input from '@/components/Input/Input.jsx'
import CustomInput from '@/components/Input/CustomInput.jsx'

## Sobre el componente

Este componente se puede utilizar para permitir a los usuarios ingresar sus datos en función de varios tipos de entrada, como texto, correo, número, contraseña, etc.

## Uso básico

Para utilizar este componente, agrega el componente `Input` con los atributos `placeholder`, `value` y `onValueChange` junto con un estado para manejar el valor del campo de texto.

<Preview>
  <CustomInput placeholder='Ingresa un texto' client:load />
</Preview>

<CodeBlock client:load>

```jsx
const [value, setValue] = useState('')

return (
  <Input
    placeholder='Ingresa un texto'
    value={value}
    onValueChange={setValue}
  />
)
```

</CodeBlock>

El código para el componente `Input` es el siguiente:

<CodeBlock client:load>

```jsx
const Input = ({
  label,
  placeholder,
  type = 'text',
  isRequired = false,
  isClearable = false,
  isInvalid = false,
  errorMessage = '',
  description = '',
  value = '',
  onValueChange,
  minLength,
  maxLength,
  pattern,
  isReadOnly = false,
  isDisabled = false,
  variant = 'default',
  color = 'default',
  rounded = 'md',
  size = 'md'
}) => {
  const handleClear = () => {
    onValueChange('')
  }

  const variants = {
    default: 'border-0 shadow-lg backdrop-blur-sm',
    bordered: 'border shadow-md',
    light: ''
  }

  const colors = {
    default: 'bg-neutral-100/20 dark:bg-zinc-700/30 dark:shadow-zinc-700/10',
    primary: 'bg-blue-500/20 ',
    secondary: 'bg-indigo-500/20 ',
    success: 'bg-green-500/30 ',
    warning: 'bg-yellow-500/40 dark:bg-yellow-500/20 ',
    danger: 'bg-red-500/20 '
  }

  const textColors = {
    default: 'text-gray-800 dark:text-gray-300',
    primary: 'text-blue-800 dark:text-blue-500',
    secondary: 'text-indigo-800 dark:text-indigo-500',
    success: 'text-green-800 dark:text-green-500',
    warning: 'text-yellow-800 dark:text-yellow-500',
    danger: 'text-red-800 dark:text-red-500'
  }

  const borderColors = {
    default: 'border-gray-600',
    primary: 'border-blue-600',
    secondary: 'border-indigo-600',
    success: 'border-green-600',
    warning: 'border-yellow-600',
    danger: 'border-red-600'
  }

  const sizes = {
    sm: 'text-xs px-2 py-1.5',
    md: 'text-sm px-3 py-2',
    lg: 'text-base px-4 py-2.5'
  }

  const roundeds = {
    none: 'rounded-none',
    sm: 'rounded-sm',
    md: 'rounded-md',
    lg: 'rounded-lg',
    full: 'rounded-full'
  }

  return (
    <div className={`flex flex-col space-y-2 ${textColors[color]}`}>
      {/* Label */}
      {label && (
        <label className='text-sm font-medium'>
          {label}
          {isRequired && <span className='text-red-500'> *</span>}
        </label>
      )}

      {/* Main Wrapper */}
      <div className='relative'>
        {/* Input Wrapper */}
        <div
          className={`flex items-center ${variants[variant]} ${
            roundeds[rounded]
          } ${variant === 'default' && colors[color]} ${borderColors[color]} `}
        >
          {/* Inner Wrapper */}
          <div className='flex-1 relative'>
            <input
              type={type}
              value={value}
              onChange={(e) => onValueChange(e.target.value)}
              required={isRequired}
              minLength={minLength}
              maxLength={maxLength}
              pattern={pattern}
              readOnly={isReadOnly}
              disabled={isDisabled}
              placeholder={placeholder}
              className={`w-full focus:outline-none ${
                variant === 'light' && 'border-b-2'
              }  ${isInvalid && 'border-2 border-red-500'} ${
                roundeds[rounded]
              } ${sizes[size]}`}
            />

            {/* Clear Button */}
            {type !== 'password' && isClearable && value && (
              <button
                type='button'
                onClick={handleClear}
                className='absolute inset-y-0 right-0 pr-3 flex items-center'
              >
                <span className='text-gray-500 hover:text-gray-700'>
                  <svg
                    xmlns='http://www.w3.org/2000/svg'
                    width='24'
                    height='24'
                    viewBox='0 0 24 24'
                    fill='currentColor'
                    className='currentColor'
                  >
                    <path stroke='none' d='M0 0h24v24H0z' fill='none' />
                    <path
                      d='M12 2l.324 .001l.318 .004l.616 .017l.299 .013l.579 .034l.553 .046c4.785 .464 6.732 2.411 7.196 7.196l.046 .553l.034 .579c.005 .098 .01 .198 .013 .299l.017 .616l.005 .642l-.005 .642l-.017 .616l-.013 .299l-.034 .579l-.046 .553c-.464 4.785 -2.411 6.732 -7.196 7.196l-.553 .046l-.579 .034c-.098 .005 -.198 .01 -.299 .013l-.616 .017l-.642 .005l-.642 -.005l-.616 -.017l-.299 -.013l-.579 -.034l-.553 -.046c-4.785 -.464 -6.732 -2.411 -7.196 -7.196l-.046 -.553l-.034 -.579a28.058 28.058 0 0 1 -.013 -.299l-.017 -.616c-.003 -.21 -.005 -.424 -.005 -.642l.001 -.324l.004 -.318l.017 -.616l.013 -.299l.034 -.579l.046 -.553c.464 -4.785 2.411 -6.732 7.196 -7.196l.553 -.046l.579 -.034c.098 -.005 .198 -.01 .299 -.013l.616 -.017c.21 -.003 .424 -.005 .642 -.005zm-1.489 7.14a1 1 0 0 0 -1.218 1.567l1.292 1.293l-1.292 1.293l-.083 .094a1 1 0 0 0 1.497 1.32l1.293 -1.292l1.293 1.292l.094 .083a1 1 0 0 0 1.32 -1.497l-1.292 -1.293l1.292 -1.293l.083 -.094a1 1 0 0 0 -1.497 -1.32l-1.293 1.292l-1.293 -1.292l-.094 -.083z'
                      fill='currentColor'
                      strokeWidth='0'
                    />
                  </svg>
                </span>
              </button>
            )}
          </div>
        </div>
      </div>

      {/* Helper Wrapper */}
      <div className='text-sm'>
        {description && <p className='font-normal'>{description}</p>}
        {isInvalid && errorMessage && (
          <p className='text-red-500'>{errorMessage}</p>
        )}
      </div>
    </div>
  )
}

export default Input
```

</CodeBlock>

## Etiqueta

Para agregar una etiqueta al componente, puedes utilizar el atributo `label` y establecer su valor.

<Preview>
  <CustomInput
    label='Label para el input'
    placeholder='Input con etiqueta'
    client:load
  />
</Preview>

<CodeBlock client:load>

```jsx
const [value, setValue] = useState('')

return (
  <Input
    label='Label para el input'
    placeholder='Input con etiqueta'
    value={value}
    onValueChange={setValue}
  />
)
```

</CodeBlock>

## Tipo de entrada

Para cambiar el tipo de entrada del componente, puedes utilizar el atributo `type` y seleccionar su valor entre `text`, `email`, `number`, `password`, `tel`, entre otros.

<Preview>
  <div className='flex flex-col gap-2'>
    <CustomInput
      label='Texto'
      placeholder='Ingresa un texto'
      type='text'
      client:load
    />
    <CustomInput
      label='Correo electrónico'
      placeholder='Ingresa un correo electrónico'
      type='email'
      client:load
    />
    <CustomInput
      label='Número'
      placeholder='Ingresa un número'
      type='number'
      client:load
    />
    <CustomInput
      label='Contraseña'
      placeholder='Ingresa una contraseña'
      type='password'
      client:load
    />
    <CustomInput
      label='Teléfono'
      placeholder='Ingresa un teléfono'
      type='tel'
      client:load
    />
  </div>
</Preview>

<CodeBlock client:load>

```jsx
<Input
  label='Texto'
  placeholder='Ingresa un texto'
  type='text'
  value={value}
  onValueChange={setValue}
/>

<Input
  label='Correo electrónico'
  placeholder='Ingresa un correo electrónico'
  type='email'
  value={value}
  onValueChange={setValue}
/>

<Input
  label='Número'
  placeholder='Ingresa un número'
  type='number'
  value={value}
  onValueChange={setValue}
/>

<Input
  label='Contraseña'
  placeholder='Ingresa una contraseña'
  type='password'
  value={value}
  onValueChange={setValue}
/>

<Input
  label='Teléfono'
  placeholder='Ingresa un teléfono'
  type='tel'
  value={value}
  onValueChange={setValue}
/>
```

</CodeBlock>

## Campo requerido

Para marcar un campo como requerido, puedes utilizar el atributo `isRequired` y establecer su valor en `true`.

<Preview>
  <CustomInput
    label='Campo requerido'
    placeholder='Ingresa un texto'
    isRequired
    client:load
  />
</Preview>

<CodeBlock client:load>

```jsx
const [value, setValue] = useState('')

return (
  <Input
    label='Campo requerido'
    placeholder='Ingresa un texto'
    isRequired
    value={value}
    onValueChange={setValue}
  />
)
```

</CodeBlock>

## Botón de borrado

Para mostrar un botón de borrado junto al componente, puedes utilizar el atributo `isClearable` y establecer su valor en `true`.

<Preview>
  <CustomInput
    label='Botón de borrado'
    placeholder='Ingresa un texto'
    isClearable
    client:load
  />
</Preview>

<CodeBlock client:load>

```jsx
const [value, setValue] = useState('')

return (
  <Input
    label='Botón de borrado'
    placeholder='Ingresa un texto'
    isClearable
    value={value}
    onValueChange={setValue}
  />
)
```

</CodeBlock>

## Validación y mensaje de error

Para mostrar un mensaje de error cuando el campo no cumple con los requisitos, puedes utilizar el atributo `isInvalid` y establecer su regla de validación, además, agregar un mensaje de error utilizando el atributo `errorMessage`.

<Preview>
  <CustomInput
    label='Validación'
    placeholder='Ingresa un texto'
    isInvalid
    errorMessage='El campo debe incluir un @'
    client:load
  />
</Preview>

<CodeBlock client:load>

```jsx
const [value, setValue] = useState('')

return (
  <Input
    label='Validación'
    placeholder='Ingresa un texto'
    isInvalid
    errorMessage='El campo debe incluir un @'
    value={value}
    onValueChange={setValue}
  />
)
```

</CodeBlock>

## Descripción

Para agregar una descripción al componente, puedes utilizar el atributo `description` y establecer su valor.

<Preview>
  <CustomInput
    label='Descripción'
    placeholder='Ingresa un texto'
    description='Este es una descripción'
    client:load
  />
</Preview>

<CodeBlock client:load>

```jsx
const [value, setValue] = useState('')

return (
  <Input
    label='Descripción'
    placeholder='Ingresa un texto'
    description='Este es una descripción'
    value={value}
    onValueChange={setValue}
  />
)
```

</CodeBlock>

## Longitud mínima y máxima

Para establecer una longitud mínima y máxima para el campo, puedes utilizar el atributo `minLength` y `maxLength` respectivamente.

<Preview>
  <CustomInput
    label='Longitud mínima y máxima'
    placeholder='Ingresa un texto'
    isInvalid
    isMinLength
    errorMessage={'El campo debe tener al menos 5 caracteres'}
    minLength={5}
    maxLength={10}
    client:load
  />
</Preview>

<CodeBlock client:load>

```jsx
const [value, setValue] = useState('')

return (
  <Input
    label='Longitud mínima y máxima'
    placeholder='Ingresa un texto'
    isInvalid={value.length < 5}
    errorMessage={'El campo debe tener al menos 5 caracteres'}
    minLength={5}
    maxLength={10}
    value={value}
    onValueChange={setValue}
  />
)
```

</CodeBlock>

## Patrón

Para establecer un patrón para el campo, puedes utilizar el atributo `pattern` y establecer su valor.

<Preview>
  <CustomInput
    type='tel'
    label='Patrón'
    placeholder='Ingresa un texto'
    pattern='[0-9]{3}-[0-9]{2}-[0-9]{3}'
    client:load
  />
</Preview>

<CodeBlock client:load>

```jsx
const [value, setValue] = useState('')

return (
  <Input
    type='tel'
    label='Patrón'
    placeholder='Ingresa un texto'
    pattern='[0-9]{3}-[0-9]{2}-[0-9]{3}'
    value={value}
    onValueChange={setValue}
  />
)
```

</CodeBlock>

## Solo lectura

Para hacer el campo de texto de solo lectura, puedes utilizar el atributo `isReadOnly` y establecer su valor en `true`.

<Preview>
  <CustomInput
    label='Solo lectura'
    placeholder='Ingresa un texto'
    isReadOnly
    client:load
  />
</Preview>

<CodeBlock client:load>

```jsx
const [value, setValue] = useState('')

return (
  <Input
    label='Solo lectura'
    placeholder='Ingresa un texto'
    isReadOnly
    value={value}
    onValueChange={setValue}
  />
)
```

</CodeBlock>

## Deshabilitado

Para deshabilitar el componente, puedes utilizar el atributo `isDisabled` y establecer su valor en `true`.

<Preview>
  <CustomInput
    label='Deshabilitado'
    placeholder='Ingresa un texto'
    isDisabled
    client:load
  />
</Preview>

<CodeBlock client:load>

```jsx
const [value, setValue] = useState('')

return (
  <Input
    label='Deshabilitado'
    placeholder='Ingresa un texto'
    isDisabled
    value={value}
    onValueChange={setValue}
  />
)
```

</CodeBlock>

## Variantes

Este componente posee algunas variantes que puedes utilizar según tus necesidades. Para esto, puedes utilizar el atributo `variant` y cambiar su valor entre `default`, `bordered` o `light`.

<Preview>
  <div className='flex flex-col gap-2'>
    <CustomInput
      label='Variante default'
      placeholder='Ingresa un texto'
      variant='default'
      client:load
    />
    <CustomInput
      label='Variante bordeado'
      placeholder='Ingresa un texto'
      variant='bordered'
      client:load
    />
    <CustomInput
      label='Variante light'
      placeholder='Ingresa un texto'
      variant='light'
      client:load
    />
  </div>
</Preview>

<CodeBlock client:load>

```jsx
<Input
  label='Variante default'
  placeholder='Ingresa un texto'
  variant='default'
  value={value}
  onValueChange={setValue}
/>

<Input
  label='Variante bordeado'
  placeholder='Ingresa un texto'
  variant='bordered'
  value={value}
  onValueChange={setValue}
/>

<Input
  label='Variante light'
  placeholder='Ingresa un texto'
  variant='light'
  value={value}
  onValueChange={setValue}
/>
```

</CodeBlock>

## Colores

Para cambiar el color del componente, puedes utilizar el atributo `color` y seleccionar su valor entre `default`, `primary`, `secondary`, `success`, `warning` o `danger`.

<Preview>
  <div className='flex flex-col gap-2'>
    <CustomInput
      label='Color default'
      placeholder='Ingresa un texto'
      color='default'
      client:load
    />
    <CustomInput
      label='Color primary'
      placeholder='Ingresa un texto'
      color='primary'
      client:load
    />
    <CustomInput
      label='Color secondary'
      placeholder='Ingresa un texto'
      color='secondary'
      client:load
    />
    <CustomInput
      label='Color success'
      placeholder='Ingresa un texto'
      color='success'
      client:load
    />
    <CustomInput
      label='Color warning'
      placeholder='Ingresa un texto'
      color='warning'
      client:load
    />
    <CustomInput
      label='Color danger'
      placeholder='Ingresa un texto'
      color='danger'
      client:load
    />
  </div>
</Preview>

<CodeBlock client:load>

```jsx
<Input
  label='Color default'
  placeholder='Ingresa un texto'
  color='default'
  value={value}
  onValueChange={setValue}
/>

<Input
  label='Color primary'
  placeholder='Ingresa un texto'
  color='primary'
  value={value}
  onValueChange={setValue}
/>

<Input
  label='Color secondary'
  placeholder='Ingresa un texto'
  color='secondary'
  value={value}
  onValueChange={setValue}
/>

<Input
  label='Color success'
  placeholder='Ingresa un texto'
  color='success'
  value={value}
  onValueChange={setValue}
/>

<Input
  label='Color warning'
  placeholder='Ingresa un texto'
  color='warning'
  value={value}
  onValueChange={setValue}
/>

<Input
  label='Color danger'
  placeholder='Ingresa un texto'
  color='danger'
  value={value}
  onValueChange={setValue}
/>
```

</CodeBlock>

## Redondeado

Para cambiar el redondeo del componente, puedes utilizar el atributo `rounded` y seleccionar su valor entre `none`, `sm`, `md`, `lg` o `full`.

<Preview>
  <div className='flex flex-col gap-2'>
    <CustomInput
      label='Redondeado none'
      placeholder='Ingresa un texto'
      rounded='none'
      client:load
    />
    <CustomInput
      label='Redondeado sm'
      placeholder='Ingresa un texto'
      rounded='sm'
      client:load
    />
    <CustomInput
      label='Redondeado md'
      placeholder='Ingresa un texto'
      rounded='md'
      client:load
    />
    <CustomInput
      label='Redondeado lg'
      placeholder='Ingresa un texto'
      rounded='lg'
      client:load
    />
    <CustomInput
      label='Redondeado full'
      placeholder='Ingresa un texto'
      rounded='full'
      client:load
    />
  </div>
</Preview>

<CodeBlock client:load>

```jsx
<Input
  label='Redondeado none'
  placeholder='Ingresa un texto'
  rounded='none'
  value={value}
  onValueChange={setValue}
/>

<Input
  label='Redondeado sm'
  placeholder='Ingresa un texto'
  rounded='sm'
  value={value}
  onValueChange={setValue}
/>

<Input
  label='Redondeado md'
  placeholder='Ingresa un texto'
  rounded='md'
  value={value}
  onValueChange={setValue}
/>

<Input
  label='Redondeado lg'
  placeholder='Ingresa un texto'
  rounded='lg'
  value={value}
  onValueChange={setValue}
/>

<Input
  label='Redondeado full'
  placeholder='Ingresa un texto'
  rounded='full'
  value={value}
  onValueChange={setValue}
/>
```

</CodeBlock>

## Tamaños

Para cambiar el tamaño del componente, puedes utilizar el atributo `size` y seleccionar su valor entre `sm`, `md` o `lg`.

<Preview>
  <div className='flex flex-col gap-2'>
    <CustomInput
      label='Tamaño sm'
      placeholder='Ingresa un texto'
      size='sm'
      client:load
    />
    <CustomInput
      label='Tamaño md'
      placeholder='Ingresa un texto'
      size='md'
      client:load
    />
    <CustomInput
      label='Tamaño lg'
      placeholder='Ingresa un texto'
      size='lg'
      client:load
    />
  </div>
</Preview>

<CodeBlock client:load>

```jsx
<Input
  label='Tamaño sm'
  placeholder='Ingresa un texto'
  size='sm'
  value={value}
  onValueChange={setValue}
/>

<Input
  label='Tamaño md'
  placeholder='Ingresa un texto'
  size='md'
  value={value}
  onValueChange={setValue}
/>

<Input
  label='Tamaño lg'
  placeholder='Ingresa un texto'
  size='lg'
  value={value}
  onValueChange={setValue}
/>
```

</CodeBlock>
