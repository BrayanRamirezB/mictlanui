---
name: Tooltip
description: Permite al usuario mostrar información sobre un elemento
---

import CodeBlock from '@/components/landing/CodeBlock.jsx'
import Preview from '@/components/landing/Preview.astro'
import Button from '@/components/react/Button.jsx'
import Tooltip from '@/components/react/Tooltip.jsx'
import CodeTabs from '@/components/landing/CodeTabs.jsx'
import { TooltipReact } from '@/const/componentsReact'
import { TooltipTs } from '@/const/componentsTs'

## Sobre el componente

Este componente permite al usuario mostrar información contextual o algun tipo de ayuda para comprender mejor un elemento.

## Uso básico

Para utilizar el componente, simplemente debemos definir el atributo `content` del componente `Tooltip`.

<Preview>
  <div class='flex items-center justify-center gap-x-2 w-full m-auto'>
    <Tooltip client:load content='Este es un tooltip'>
      <Button>Botón con tooltip</Button>
    </Tooltip>
  </div>
</Preview>

<CodeBlock client:load>

```jsx
<Tooltip content='Este es un tooltip'>
  <Button>Botón con tooltip</Button>
</Tooltip>
```

</CodeBlock>

El código para el componente `Tooltip` es el siguiente:

<CodeTabs
  client:load
  tabs={[
    { label: 'React', code: TooltipReact },
    { label: 'TypeScript', code: TooltipTs }
  ]}
/>

## Posicionamiento

Para cambiar el posicionamiento del componente, se puede utilizar el atributo `placement` en el componente `Tooltip`. Este atributo los siguientes valores: `top`, `bottom`, `left` y `right`.

<Preview>
  <div class='flex flex-col items-center justify-center gap-4 w-full m-auto'>
    <Tooltip client:load content='Este es un tooltip' placement='right'>
      <Button>Right tooltip</Button>
    </Tooltip>

    <Tooltip client:load content='Este es un tooltip' placement='left'>
      <Button>Left tooltip</Button>
    </Tooltip>

    <Tooltip client:load content='Este es un tooltip' placement='bottom'>
      <Button>Bottom tooltip</Button>
    </Tooltip>

    <Tooltip client:load content='Este es un tooltip' placement='top'>
      <Button>Top tooltip</Button>
    </Tooltip>

  </div>
</Preview>

<CodeBlock client:load>

```jsx
<Tooltip content='Este es un tooltip' placement='right'>
  <Button>Right tooltip</Button>
</Tooltip>

<Tooltip content='Este es un tooltip' placement='left'>
  <Button>Left tooltip</Button>
</Tooltip>

<Tooltip content='Este es un tooltip' placement='bottom'>
  <Button>Bottom tooltip</Button>
</Tooltip>

<Tooltip content='Este es un tooltip' placement='top'>
  <Button>Top tooltip</Button>
</Tooltip>
```

</CodeBlock>

## Color

Este componente tiene un atributo para cambiar su color. Para esto tenemos los siguientes valores: `default`, `primary`, `secondary`, `success`, `warning` y `danger`.

<Preview>
  <div class='flex flex-col items-center justify-center gap-4 w-full m-auto'>
    <Tooltip client:load content='Este es un tooltip' color='default'>
      <Button>Default tooltip</Button>
    </Tooltip>

    <Tooltip client:load content='Este es un tooltip' color='primary'>
      <Button>Primary tooltip</Button>
    </Tooltip>

    <Tooltip client:load content='Este es un tooltip' color='secondary'>
      <Button>Secondary tooltip</Button>
    </Tooltip>

    <Tooltip client:load content='Este es un tooltip' color='success'>
      <Button>Success tooltip</Button>
    </Tooltip>

    <Tooltip client:load content='Este es un tooltip' color='warning'>
      <Button>Warning tooltip</Button>
    </Tooltip>

    <Tooltip client:load content='Este es un tooltip' color='danger'>
      <Button>Danger tooltip</Button>
    </Tooltip>

  </div>
</Preview>

<CodeBlock client:load>

```jsx
<Tooltip content='Este es un tooltip' color='default'>
  <Button>Default tooltip</Button>
</Tooltip>

<Tooltip content='Este es un tooltip' color='primary'>
  <Button>Primary tooltip</Button>
</Tooltip>

<Tooltip content='Este es un tooltip' color='secondary'>
  <Button>Secondary tooltip</Button>
</Tooltip>

<Tooltip content='Este es un tooltip' color='success'>
  <Button>Success tooltip</Button>
</Tooltip>

<Tooltip content='Este es un tooltip' color='warning'>
  <Button>Warning tooltip</Button>
</Tooltip>

<Tooltip content='Este es un tooltip' color='danger'>
  <Button>Danger tooltip</Button>
</Tooltip>
```

</CodeBlock>

## Rounded

Para cambiar el borde del componente, se puede utilizar el atributo `rounded`. Este atributo tiene los siguientes valores: `none`, `sm`, `md`, `lg` y `full`.

<Preview>
  <div class='flex flex-col items-center justify-center gap-4 w-full m-auto'>
    <Tooltip client:load content='Este es un tooltip' rounded='none'>
      <Button>None tooltip</Button>
    </Tooltip>

    <Tooltip client:load content='Este es un tooltip' rounded='sm'>
      <Button>Small tooltip</Button>
    </Tooltip>

    <Tooltip client:load content='Este es un tooltip' rounded='md'>
      <Button>Medium tooltip</Button>
    </Tooltip>

    <Tooltip client:load content='Este es un tooltip' rounded='lg'>
      <Button>Large tooltip</Button>
    </Tooltip>

    <Tooltip client:load content='Este es un tooltip' rounded='full'>
      <Button>Full tooltip</Button>
    </Tooltip>

  </div>
</Preview>

<CodeBlock client:load>

```jsx
<Tooltip content='Este es un tooltip' rounded='none'>
  <Button>None tooltip</Button>
</Tooltip>

<Tooltip content='Este es un tooltip' rounded='sm'>
  <Button>Small tooltip</Button>
</Tooltip>

<Tooltip content='Este es un tooltip' rounded='md'>
  <Button>Medium tooltip</Button>
</Tooltip>

<Tooltip content='Este es un tooltip' rounded='lg'>
  <Button>Large tooltip</Button>
</Tooltip>

<Tooltip content='Este es un tooltip' rounded='full'>
  <Button>Full tooltip</Button>
</Tooltip>
```

</CodeBlock>

## Delay

Para cambiar el tiempo de espera antes de mostrar el componente, se puede utilizar el atributo `delay`. Este atributo acepta un valor en milisegundos.

<Preview>
  <div class='flex flex-col items-center justify-center gap-4 w-full m-auto'>
    <Tooltip client:load content='Este es un tooltip' delay={1000}>
      <Button>Tooltip with delay 1000ms</Button>
    </Tooltip>

    <Tooltip client:load content='Este es un tooltip' delay={2000}>
      <Button>Tooltip with delay 2000ms</Button>
    </Tooltip>

    <Tooltip client:load content='Este es un tooltip' delay={3000}>
      <Button>Tooltip with delay 3000ms</Button>
    </Tooltip>

  </div>
</Preview>

<CodeBlock client:load>

```jsx
<Tooltip content='Este es un tooltip' delay={1000}>
  <Button>Tooltip with delay 1000ms</Button>
</Tooltip>

<Tooltip content='Este es un tooltip' delay={2000}>
  <Button>Tooltip with delay 2000ms</Button>
</Tooltip>

<Tooltip content='Este es un tooltip' delay={3000}>
  <Button>Tooltip with delay 3000ms</Button>
</Tooltip>
```

</CodeBlock>

## Deshabilitado

Para deshabilitar el componente, se puede utilizar el atributo `isDisabled = {true}` en el componente `Tooltip`.

<Preview>
  <div class='flex flex-col items-center justify-center gap-4 w-full m-auto'>
    <Tooltip client:load content='Este es un tooltip' isDisabled={true}>
      <Button>Disabled tooltip</Button>
    </Tooltip>
  </div>
</Preview>

<CodeBlock client:load>

```jsx
<Tooltip content='Este es un tooltip' isDisabled={true}>
  <Button>Disabled tooltip</Button>
</Tooltip>
```

</CodeBlock>
