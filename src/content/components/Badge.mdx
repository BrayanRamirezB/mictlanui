---
name: 'Badge'
description: 'Un componente que muestra un valor en forma de etiqueta.'
---

import CodeBlock from '@/components/landing/CodeBlock.jsx'
import Preview from '@/components/landing/Preview.astro'
import Badge from '@/components/react/Badge.jsx'
import CodeTabs from '@/components/landing/CodeTabs.jsx'
import { BadgeReact } from '@/const/componentsReact'
import { BadgeTs } from '@/const/componentsTs'
import { BadgeAstro } from '@/const/componentsAstro'

## Sobre el componente

Este componente se utiliza para mostrar información relevante a los usuarios como etiquetas, íconos o botones.

## Uso básico

Para utilizar el componente, simplemente debes definir el atributo `text`.

<Preview>
  <div class='flex justify-center items-center gap-x-2 w-full m-auto'>
    <Badge client:load text='Default' />
  </div>
</Preview>

<CodeBlock client:load>

```jsx
<Badge text='Default' />
```

</CodeBlock>

El código para `Badge` es el siguiente:

<CodeTabs
  client:load
  tabs={[
    { label: 'React', code: BadgeReact },
    { label: 'TypeScript', code: BadgeTs },
    { label: 'Astro', code: BadgeAstro }
  ]}
/>

## Tipos de etiquetas

Para cambiar el estilo del componente, se puede utilizar el atributo `type` en el componente `Badge`. Este atributo acepta los siguientes valores: `default`, `icon` y `bordered`.

<Preview>
  <div class='flex justify-center items-center gap-x-2 w-full m-auto'>
    <Badge client:load text='Default' type='default' />
    <Badge client:load type='icon' />
    <Badge client:load text='Bordered' type='bordered' />
  </div>
</Preview>

<CodeBlock client:load>

```jsx
<Badge text='Default' type='default' text='Default' />
<Badge text='Icon' type='icon' />
<Badge text='Bordered' type='bordered' text='Bordered' />
```

</CodeBlock>

## Color

Para cambiar el color del componente, se puede utilizar el atributo `color` en el componente `Badge`. Este atributo acepta los siguientes valores: `default`, `primary`, `secondary`, `success`, `warning` y `danger`.

<Preview>
  <div className='flex flex-col gap-2'>
    <div class='flex justify-center items-center gap-x-2 w-full m-auto'>
      <Badge client:load text='Default' color='default' />
      <Badge client:load text='Primary' color='primary' />
      <Badge client:load text='Secondary' color='secondary' />
      <Badge client:load text='Success' color='success' />
      <Badge client:load text='Warning' color='warning' />
      <Badge client:load text='Danger' color='danger' />
    </div>
    <div class='flex justify-center items-center gap-x-2 w-full m-auto'>
      <Badge client:load type='icon' color='default' />
      <Badge client:load type='icon' color='primary' />
      <Badge client:load type='icon' color='secondary' />
      <Badge client:load type='icon' color='success' />
      <Badge client:load type='icon' color='warning' />
      <Badge client:load type='icon' color='danger' />
    </div>
    <div class='flex justify-center items-center gap-x-2 w-full m-auto'>
      <Badge client:load type='bordered' text='default' color='default' />
      <Badge client:load type='bordered' text='primary' color='primary' />
      <Badge client:load type='bordered' text='secondary' color='secondary' />
      <Badge client:load type='bordered' text='success' color='success' />
      <Badge client:load type='bordered' text='warning' color='warning' />
      <Badge client:load type='bordered' text='danger' color='danger' />
    </div>
  </div>
</Preview>

<CodeBlock client:load>

```jsx
<Badge text='Default' color='default' />
<Badge text='Primary' color='primary' />
<Badge text='Secondary' color='secondary' />
<Badge text='Success' color='success' />
<Badge text='Warning' color='warning' />
<Badge text='Danger' color='danger' />

<Badge type='icon' color='default' />
<Badge type='icon' color='primary' />
<Badge type='icon' color='secondary' />
<Badge type='icon' color='success' />
<Badge type='icon' color='warning' />
<Badge type='icon' color='danger' />

<Badge type='bordered' text='default' color='default' />
<Badge type='bordered' text='primary' color='primary' />
<Badge type='bordered' text='secondary' color='secondary' />
<Badge type='bordered' text='success' color='success' />
<Badge type='bordered' text='warning' color='warning' />
<Badge type='bordered' text='danger' color='danger' />
```

</CodeBlock>

## Tamaños

Para cambiar el tamaño del componente, se puede utilizar el atributo `size` en el componente `Badge`. Este atributo acepta los siguientes valores: `sm`, `md` y `lg`.

<Preview>
  <div class='flex justify-center items-center gap-x-2 w-full m-auto'>
    <Badge client:load text='Size sm' color='primary' size='sm' />
    <Badge client:load text='Size md' color='primary' size='md' />
    <Badge client:load text='Size lg' color='primary' size='lg' />
  </div>
</Preview>

<CodeBlock client:load>

```jsx
<Badge text='Size sm' color='primary' size='sm' />
<Badge text='Size md' color='primary' size='md' />
<Badge text='Size lg' color='primary' size='lg' />
```

</CodeBlock>

## Rounded

Para cambiar el estilo del componente, se puede utilizar el atributo `rounded` en el componente `Badge`. Este atributo acepta los siguientes valores: `none`, `sm`, `md`, `lg` y `full`.

<Preview>
  <div class='flex justify-center items-center gap-x-2 w-full m-auto'>
    <Badge client:load text='Rounded none' color='secondary' rounded='none' />
    <Badge client:load text='Rounded sm' color='secondary' rounded='sm' />
    <Badge client:load text='Rounded md' color='secondary' rounded='md' />
    <Badge client:load text='Rounded lg' color='secondary' rounded='lg' />
    <Badge client:load text='Rounded full' color='secondary' rounded='full' />
  </div>
</Preview>

<CodeBlock client:load>

```jsx
<Badge text='Rounded none' color='secondary' rounded='none' />
<Badge text='Rounded sm' color='secondary' rounded='sm' />
<Badge text='Rounded md' color='secondary' rounded='md' />
<Badge text='Rounded lg' color='secondary' rounded='lg' />
<Badge text='Rounded full' color='secondary' rounded='full' />
```

</CodeBlock>

## Puntos

Para agregar puntos al componente, simplemente debes agregar el atributo `dot={true}` en el componente `Badge`.

<Preview>
  <div class='flex justify-center items-center gap-x-2 w-full m-auto'>
    <Badge client:load text='Default' color='success' dot={true} />
  </div>
</Preview>

<CodeBlock client:load>

```jsx
<Badge text='Default' color='success' dot={true} />
```

</CodeBlock>

### Posición de puntos

Para cambiar la posición de los puntos del componente, se puede utilizar el atributo `dotPosition` en el componente `Badge`. Este atributo acepta los siguientes valores: `top-left`, `top-right`, `bottom-left` y `bottom-right`.

<Preview>
  <div class='flex justify-center items-center gap-x-2 w-full m-auto'>
    <Badge
      client:load
      text='Default'
      color='warning'
      dot={true}
      dotPosition='top-left'
    />
    <Badge
      client:load
      text='Default'
      color='warning'
      dot={true}
      dotPosition='top-right'
    />
    <Badge
      client:load
      text='Default'
      color='warning'
      dot={true}
      dotPosition='bottom-left'
    />
    <Badge
      client:load
      text='Default'
      color='warning'
      dot={true}
      dotPosition='bottom-right'
    />
  </div>
</Preview>

<CodeBlock client:load>

```jsx
<Badge text='Default' color='warning' dot={true} dotPosition='top-left' />
<Badge text='Default' color='warning' dot={true} dotPosition='top-right' />
<Badge text='Default' color='warning' dot={true} dotPosition='bottom-left' />
<Badge text='Default' color='warning' dot={true} dotPosition='bottom-right' />
```

</CodeBlock>

### Color de puntos

Para cambiar el color de los puntos del componente, se puede utilizar el atributo `dotColor` en el componente `Badge`. Este atributo acepta los siguientes valores: `default`, `primary`, `secondary`, `success`, `warning` y `danger`.

<Preview>
  <div class='flex justify-center items-center gap-x-2 w-full m-auto'>
    <Badge client:load text='Default' dot={true} dotColor='default' />
    <Badge client:load text='Default' dot={true} dotColor='primary' />
    <Badge client:load text='Default' dot={true} dotColor='secondary' />
    <Badge client:load text='Default' dot={true} dotColor='success' />
    <Badge client:load text='Default' dot={true} dotColor='warning' />
    <Badge client:load text='Default' dot={true} dotColor='danger' />
  </div>
</Preview>

<CodeBlock client:load>

```jsx
<Badge text='Default' dot={true} dotColor='default' />
<Badge text='Default' dot={true} dotColor='primary' />
<Badge text='Default' dot={true} dotColor='secondary' />
<Badge text='Default' dot={true} dotColor='success' />
<Badge text='Default' dot={true} dotColor='warning' />
<Badge text='Default' dot={true} dotColor='danger' />
```

</CodeBlock>

### Texto de puntos

Para cambiar el texto de los puntos del componente, se puede utilizar el atributo `dotText` en el componente `Badge`. Este atributo acepta cualquier valor.

<Preview>
  <div class='flex justify-center items-center gap-x-2 w-full m-auto'>
    <Badge client:load text='Default' dot={true} dotText='2' />
  </div>
</Preview>

<CodeBlock client:load>

```jsx
<Badge text='Default' dot={true} dotText='2' />
```

</CodeBlock>

## Tipos de iconos

Para cambiar el estilo del componente, se puede utilizar el atributo `icon` en el componente `Badge`.

<Preview>
  <div class='flex justify-center items-center gap-x-2 w-full m-auto'>
    <Badge client:load type='icon' icon='😀' />
    <Badge client:load type='icon' color='primary' icon='😎' />
    <Badge client:load type='icon' color='secondary' icon='🤓' />
    <Badge client:load type='icon' color='success' icon='👍 like' />
    <Badge client:load type='icon' color='danger' icon='👎 dislike' />
  </div>
</Preview>

<CodeBlock client:load>

```jsx
<Badge type='icon' icon='😀' />
<Badge type='icon' color='primary' icon='😎' />
<Badge type='icon' color='secondary' icon='🤓' />
<Badge type='icon' color='success' icon='👍 like' />
<Badge type='icon' color='danger' icon='👎 dislike' />
```

</CodeBlock>
